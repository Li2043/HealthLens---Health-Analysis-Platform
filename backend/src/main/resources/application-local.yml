spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:healthlens}
    username: ${POSTGRES_USER:healthlens}
    password: ${POSTGRES_PASSWORD:change_me}

healthlens:
  ai-service:
    base-url: ${AI_SERVICE_BASE_URL:http://localhost:8000}

logging:
  level:
    com.healthlens.platform: INFO
