spring:
  config:
    activate:
      on-profile: prod

healthlens:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}

logging:
  level:
    com.healthlens.platform: INFO
